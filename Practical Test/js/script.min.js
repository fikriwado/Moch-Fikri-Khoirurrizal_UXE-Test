const wrapSlider = document.querySelector(".wrapper-slider"), btnSlider = document.querySelectorAll(".btn-slider"), sliderItems = document.querySelector(".slider-items"), numberOfSliderItems = document.querySelectorAll(".slider-items .card"), sliderOffset = numberOfSliderItems.length * (numberOfSliderItems[0].offsetWidth + 20); let translateX = 0; btnSlider.forEach(a => { a.addEventListener("click", a => { "previous" === a.target.id ? translateX > -30 && translateX < 30 ? translateX = 4 : translateX += 30 : translateX > wrapSlider.offsetWidth - sliderOffset ? translateX -= 30 : translateX = wrapSlider.offsetWidth - sliderOffset + 16, sliderItems.style.transform = `translateX(${translateX}px)`, translateX < 0 ? btnSlider[0].style.display = "block" : btnSlider[0].style.display = "none", translateX === wrapSlider.offsetWidth - sliderOffset + 16 ? btnSlider[1].style.display = "none" : btnSlider[1].style.display = "block" }) }); const btnCollapse = document.getElementById("collapse-footer"), contentCollapse = document.getElementById("content-collapse-footer"); btnCollapse.addEventListener("click", a => { a.preventDefault(), contentCollapse.classList.toggle("active"), btnCollapse.classList.toggle("active"), 'Collapse all <i class="fa-solid fa-angle-up"></i>' === btnCollapse.innerHTML ? btnCollapse.innerHTML = 'Show all <i class="fa-solid fa-angle-down"></i>' : btnCollapse.innerHTML = 'Collapse all <i class="fa-solid fa-angle-up"></i>' }); const rootElement = document.documentElement, btnBackTop = document.querySelector(".back-to-top"); btnBackTop.addEventListener("click", () => { rootElement.scrollTo({ top: 0, behavior: "smooth" }) })